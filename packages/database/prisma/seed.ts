/**
 * Seed is intentionally a no-op for production-style local use.
 * Create a real account via /signup, then connect Concept2 with your Logbook.
 *
 * Historical synthetic marathon fixtures were removed from the default path.
 */
console.log('No seed data. Sign up at /signup and connect your Concept2 Logbook in Settings.');
process.exit(0);
